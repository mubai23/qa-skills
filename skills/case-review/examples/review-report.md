# 用例评审报告：登录功能

## 评审概览

| 维度 | 结果 |
|------|------|
| 评审用例数 | 16 条（P0: 6 / P1: 10） |
| 覆盖完整性 | 遗漏 2 项 |
| 用例质量 | 3 条问题，涉及 2 条用例 |
| 优先级合理性 | 2 条需调整 |
| 冗余 | 1 条需处理 |

---

## 一、覆盖漏洞（严重）

| # | 缺失场景 | 应覆盖 BR | 风险说明 | 建议补充 |
|---|---------|----------|---------|---------|
| 1 | 用户名/密码输入框 SQL 注入测试 | R-005 | 存在 SQL 注入风险 | 增加用例：用户名输入 `' OR '1'='1`，预期登录失败且不返回数据库错误 |
| 2 | 用户名/密码输入框 XSS 测试 | R-005 | 存在 XSS 风险 | 增加用例：用户名输入 `<script>alert(1)</script>`，预期登录失败且不弹窗 |

---

## 二、用例质量问题（一般）

| 用例编号 | 问题类型 | 具体问题 | 修改建议 |
|---------|---------|---------|---------|
| LOGIN-008 | 步骤可执行性不足 | 一条用例包含 4 个边界测试（3/4/32/33位），应拆分或明确是参数化执行 | 拆分为 4 条独立用例，或标注"参数化执行，每条用不同长度值" |
| LOGIN-009 | 步骤可执行性不足 | 同上，一条用例包含 4 个边界测试 | 同 LOGIN-008 |
| LOGIN-014 | 前置条件不完整 | 未说明需要等多久，7天免登录无法在测试周期内完成 | 备注说明：短期验证可通过修改 Token 过期时间模拟 |

---

## 三、优先级问题

| 用例编号 | 当前 | 建议 | 原因 |
|---------|------|------|------|
| LOGIN-003 | P0 | P1 | "用户名不存在"失败不阻断核心流程，属于异常分支而非主路径 |
| LOGIN-004 | P0 | P1 | 用户名空值校验属于边界异常，用户可立即修正，不阻断功能 |

调整后 P0: 4/16 = 25%（仍略超 20%，但 LOGIN-001/002/006/011 均为真正的阻断级）

---

## 四、冗余

| 用例编号组 | 重复内容 | 覆盖相同 BR | 建议处理 |
|-----------|---------|------------|---------|
| LOGIN-005 / LOGIN-016 | 都是"密码为空"，仅用户名有无差异 | BR-005 | 保留 LOGIN-005，删除 LOGIN-016 或合并为"密码为空（有/无用户名各一次）" |

---

## 五、改进建议

- 建议每个用例标注对应 BR 编号，便于追溯（LOGIN-003/010 已在备注中体现，其余应补全）
- LOGIN-008/009 建议拆分为独立用例，方便执行和缺陷关联

---

## 六、评审结论

- [ ] 可直接执行
- [x] **修改后执行** — 补充 SQL 注入/XSS 用例，修正 2 条优先级，合并 1 条冗余，修改后无需再次评审
- [ ] 需重新设计
